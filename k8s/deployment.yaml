apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: all-in-market
  labels:
    app: cart-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-service
  template:
    metadata:
      name: cart-service
      labels:
        app: cart-service
    spec:
      containers:
        - name: cart-service
          image: albertsereda/all-in-market-cart-service
          imagePullPolicy: Always
          env:
            - name: SPRING_CONFIG_IMPORT
              value: file:/config/
          ports:
            - containerPort: 8080
              protocol: TCP
          volumeMounts:
            - name: cart-service-config
              mountPath: /config
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "500m"
      volumes:
        - name: cart-service-config
          configMap:
            name: cart-service-config
      restartPolicy: Always